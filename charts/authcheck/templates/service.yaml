apiVersion: v1
kind: Service
metadata:
  name: {{ include "authcheck.fullname" . }}
  labels:
    {{- include "authcheck.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "authcheck.selectorLabels" . | nindent 4 }}
